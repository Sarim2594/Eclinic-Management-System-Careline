version: '3.8'

services:
  db:
    image: postgres:18-alpine
    container_name: careline-db
    environment:
      POSTGRES_DB: careline
      POSTGRES_USER: neondb_owner
      POSTGRES_PASSWORD: careline_pass
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./careline_dump.sql:/docker-entrypoint-initdb.d/careline_dump.sql
    ports:
      - "5432:5432"
    networks:
      - careline-network

  backend:
    build: ./backend
    container_name: careline-backend
    environment:
      DATABASE_URL: postgresql://neondb_owner:careline_pass@db:5432/careline
      NODE_ENV: production
      APP_HOST: 0.0.0.0
      APP_PORT: 5000
    ports:
      - "5000:5000"
    depends_on:
      - db
    networks:
      - careline-network

  frontend:
    build: ./frontend
    restart: always
    container_name: careline-frontend
    expose:
      - "80"
    depends_on:
      - backend
    networks:
      - careline-network

volumes:
  postgres_data:

networks:
  careline-network:
    driver: bridge